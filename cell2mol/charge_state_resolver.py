from cell2mol.classes.charge_state import ChargeState
import logging

logger = logging.getLogger(__name__)


def enumerate_charge_states() -> list[ChargeState]:
    """Enumerate possible charge states for the specie."""
    return
